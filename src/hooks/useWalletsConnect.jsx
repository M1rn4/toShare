import {
  // import the wallet you want to connect
  metamaskWallet,
  coinbaseWallet,
  useConnect,
} from "@thirdweb-dev/react";

export default function useWalletsConnect() {
  const connect = useConnect();

  const walletConfig = metamaskWallet();
  const 

  return (

  )
}